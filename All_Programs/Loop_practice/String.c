#include<stdio.h>
#include<string.h>
main()
{
	char arr[20]="Arvind Suthar";
	int i;
	int count = 0;
	
	for(i=0;i<arr[i];i++)
	{ 
		count++;
	}
		printf("length function: %d",strlen(arr));
		
		printf("\nString Length: %d",count);

}
