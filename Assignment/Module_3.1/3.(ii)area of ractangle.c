#include<stdio.h>
main()
{
	int length,breath;
	printf("Enter the length of rectangle:");    // find area of ractangle
	scanf("%d",&length);
	printf("Enter the breath of ractangle:");
	scanf("%d",&breath);
	printf("\nThe are of ractangle is : %d",length*breath);
	
}
